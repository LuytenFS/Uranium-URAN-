# Uranium (URAN & ATOM) -

URAN (Uranium) is a 64-bit monolithic x86_64 kernel built from the boot sector up, prioritizing total machine sovereignty and the removal of high-level abstractions. ATOM is the native task-splitting shell designed to interact with the kernel nucleus. It uses QEMU, and does not yet utilize .iso for the finalized os image.
